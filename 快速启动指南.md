# 🚀 快速启动指南

## 一键启动（推荐）

### 步骤 1: 启动后端

打开 PowerShell，执行：

```powershell
cd D:\users\nxy\deep_search_agent\backend
.\start.ps1
```

看到以下信息表示启动成功：
```
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
INFO:     Application startup complete.
```

### 步骤 2: 启动前端

**新开一个 PowerShell 窗口**，执行：

```powershell
cd D:\users\nxy\deep_search_agent\frontend
.\start.ps1
```

看到以下信息表示启动成功：
```
  ➜  Local:   http://localhost:5173/
```

### 步骤 3: 打开浏览器

访问: **http://localhost:5173**

---

## 🎯 使用说明

1. 在输入框中输入研究主题，例如：
   - "人工智能在医疗领域的应用"
   - "2024年最新的量子计算进展"
   - "区块链技术的实际应用场景"

2. 选择搜索引擎（可选，默认使用后端配置）

3. 点击"开始研究"按钮

4. 等待系统：
   - 自动分解研究任务
   - 执行网络搜索
   - 总结每个任务的结果
   - 生成最终研究报告

---

## ⚠️ 常见问题

### Q1: 后端启动失败，显示 "Connection error"

**解决方法:** 使用 `start.ps1` 脚本启动，它会自动清除代理设置。

### Q2: 显示乱码或编码错误

**解决方法:** `start.ps1` 已自动设置 UTF-8 编码，确保使用脚本启动。

### Q3: 前端无法连接后端

**解决方法:** 
1. 确认后端已启动（访问 http://localhost:8000/docs 查看 API 文档）
2. 检查防火墙设置
3. 查看浏览器控制台错误信息

### Q4: API 调用失败

**可能原因:**
1. API 密钥无效或过期
2. 网络连接问题
3. API 配额用完

**解决方法:**
1. 检查 `backend/.env` 文件中的 `LLM_API_KEY`
2. 访问 https://platform.deepseek.com 确认密钥状态

---

## 🛑 停止服务

在运行服务的 PowerShell 窗口中按 `Ctrl + C`

---

## 📚 更多信息

- 完整文档: 查看项目根目录的 `README.md`
- 问题诊断: 查看 `问题修复报告.md`
- API 文档: http://localhost:8000/docs (后端启动后访问)

---

## 🔧 高级选项

### 手动启动（不使用脚本）

**后端:**
```powershell
cd backend
$env:HTTP_PROXY=''; $env:HTTPS_PROXY=''; $env:ALL_PROXY=''; $env:NO_PROXY='*'
$env:PYTHONIOENCODING='utf-8'
python src/main.py
```

**前端:**
```powershell
cd frontend
npm run dev
```

### 修改配置

编辑 `backend/.env` 文件：
```env
LLM_PROVIDER=custom
LLM_MODEL_ID=deepseek-chat
LLM_API_KEY=your-api-key-here
LLM_BASE_URL=https://api.deepseek.com/v1
SEARCH_API=duckduckgo
MAX_WEB_RESEARCH_LOOPS=3
```

保存后重启后端服务。

---

**祝使用愉快！** 🎉
